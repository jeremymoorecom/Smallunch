This folder contains the template used to link a backend module to a generated module.
Every file that exists inside the 'backend_module_auto' template folder should have a matching file inside this folder that
includes the generated file or extends the generated file.